server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      # Routes are configured in Session 6.
      routes: []

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
