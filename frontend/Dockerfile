# =============================================================
# frontend — Placeholder Dockerfile
# Replaced with a proper multi-stage Node + Nginx build in Session 7.
# =============================================================
FROM nginx:1.27-alpine
EXPOSE 80
